id: a2a-multitenant-isolation-001
info:
  name: A2A Cross-Tenant Task Content Disclosure
  author: batesian
  severity: high
  description: |
    Creates a task with unique probe text for each of two authenticated
    principals, then reads each task using the other principal's credentials.
    Reports a cross-tenant disclosure only when the returned task contains the
    owner's probe text in history, an artifact, or a status message. An echoed
    task ID or context ID alone does not establish content access. If an
    anonymous read exposes the same content, the anonymous task-read rule owns
    that finding instead.

    Requires two distinct principals configured in batesian.yaml or with
    repeated --principal flags. A confirmed finding demonstrates disclosure of
    the observed probe content; it does not assume that all task history or
    artifacts are accessible.

  references:
    - https://a2a-protocol.org/latest/specification/
    - https://owasp.org/www-project-top-ten/2021/A01_2021-Broken_Access_Control
    - https://owasp.org/www-project-api-security/
    - https://cwe.mitre.org/data/definitions/639.html
    - https://cwe.mitre.org/data/definitions/284.html

  tags:
    - a2a
    - multi-tenant
    - isolation
    - authorization
    - idor
    - chained
    - owasp-agentic-top10
    - cwe-639
    - cwe-284

attack:
  protocol: a2a
  type: a2a-multitenant-isolation

remediation: |
  1. Bind every task and contextId to the authenticated tenant at creation time,
     and verify tenant ownership on every tasks/get, tasks/cancel, and tasks/list
     request - authentication alone is not authorization.
  2. Return JSON-RPC error -32001 (TaskNotFoundError) for any task that does not
     belong to the requesting tenant; do not reveal whether the task exists.
  3. Scope task storage per tenant (separate namespaces/partitions) so a lookup
     cannot cross a tenant boundary even if an ID is guessed or leaked.
  4. Include the tenant identifier in audit logs for every task access so
     cross-tenant attempts are detectable.
